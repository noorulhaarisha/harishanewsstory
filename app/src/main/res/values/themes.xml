<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MVVMWithKotlinCoroutinesAndRetrofit" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="MySearchViewTheme" parent="Widget.AppCompat.SearchView">
        <item name="android:background">@color/white</item>
        <!-- Changes icons color -->
        <item name="android:colorForeground">@color/light_blue</item>
    </style>
    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Gets rid of the search icon -->
        <!--        <item name="searchIcon">@null</item>-->
        <!-- Gets rid of the "underline" in the text -->
        <item name="queryBackground">@color/colorContainer</item>
        <item name="background">@color/colorContainer</item>
        <!-- Gets rid of the search icon when the SearchView is expanded -->
        <item name="searchHintIcon">@null</item>
        <!-- The hint text that appears when the user has not typed anything -->
        <item name="queryHint">@string/search_stories</item>
        <item name="android:queryHint">@string/search_stories</item>
        <item name="android:background">@drawable/background_search</item>
        <!--        <item name="closeIcon">@drawable/ic_clo</item>-->
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textColorSecondary">@color/colorAccent</item>
    </style>

</resources>